# ── Dependencies ──
node_modules/
.pnpm-store/
.yarn/
.npm/

# ── Build outputs ──
dist/
build/
.next/
.nuxt/
.cache/
.parcel-cache/
*.tsbuildinfo

# ── iOS build artifacts ──
ios/App/build/
ios/App/Pods/
ios/App/DerivedData/
ios/.xcode.env.local
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
ios/App/App/public/
# Keep generated capacitor configs but exclude per-machine state

# ── Android (when added) ──
android/app/build/
android/.gradle/
android/local.properties
android/captures/
android/.idea/
*.iml

# ── Capacitor ──
.capacitor/
www-backup/

# ── Env / secrets ──
.env
.env.local
.env.*.local
.env.production
.env.development
# Keep .env.example as a template (track in git)
!.env.example

# ── Editor / OS ──
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo
Thumbs.db

# ── Logs / runtime ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Testing / coverage ──
coverage/
.nyc_output/
playwright-report/
test-results/

# ── Supabase (local emulator state, will add later) ──
supabase/.branches/
supabase/.temp/
supabase/.env

# ── Misc ──
*.tgz
.firebase/
